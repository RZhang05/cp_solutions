#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef pair<int,int> pii;

#define PB push_back
#define MP make_pair

const int MAXN=0;

int main() {
    cin.tie(0);
    cin.sync_with_stdio(0);
    int N, M; cin >> N >> M;
    if(M>=N) cout << "YES";
    else cout << "NO";
    return 0;
}

