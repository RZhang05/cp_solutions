#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef pair<int,int> pii;

#define pb push_back
#define mp make_pair
#define endl '\n'
const int MAXN = 1e6+5;
int v[MAXN];

double ans = 0;

void rec(int s) {

}

int main() {
  cin.tie(0); cin.sync_with_stdio(0);
  int N; cin >> N;
  for(int i=0;i<N;i++) {
    cin >> v[i];
  }
  rec(0);
  return 0;
}
